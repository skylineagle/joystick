{
  "name": "joystick",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "bun@1.2.10",
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "turbo run dev",
    "db:generate": "turbo run db:generate"
  },
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.5.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "inngest": "^3.35.1"
  }
}
